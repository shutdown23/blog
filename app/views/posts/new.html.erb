<h1> New Posts</h1>

<%= form_for :post, url: posts_path do |f| %>
	<%= f.label :title %>
	<br>
	<%= f.text_field :title %>

	<%= f.label :body %>
	<br>
	<%= f.text_area :body %>
	<%= f.submit %>
	<% end %>
